// src/services/auth.ts
// ─── Auth Service Interface ───────────────────────────────────────────────────
// Your app imports from here ONLY. Never import from firebase directly.
// To switch backends: swap the provider import below — nothing else changes.

export { AuthService } from '@/src/services/providers/firebase/auth';
